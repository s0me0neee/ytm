[package]
name = "lrclib"
version = "0.1.0"
edition = "2024"

[dependencies]
# The same version the rest of the workspace resolves to, so there is one HTTP
# stack rather than two.
#
# "query" because 0.13 put `RequestBuilder::query` behind a feature; on 0.12 it
# was unconditional, so a bump that leaves this out fails to compile rather than
# quietly losing anything.
#
# "gzip" because /search returns the *complete* lyrics of every hit — a single
# rung for a popular track is a quarter of a megabyte, and one lookup walks
# several. lrclib serves gzip, and reqwest only asks for it when this feature is
# on: measured 260 KB -> 23 KB on one rung. It decodes transparently, so no
# calling code changes.
reqwest = { version = "0.13.4", features = ["json", "gzip", "query"] }
serde = { version = "1.0.229", features = ["derive"] }
# Per-record deserialisation, so one malformed row can't fail a whole response.
serde_json = "1.0.151"
log = "0.4.33"
thiserror = "2.0.20"

[dev-dependencies]
# Only the tests need a runtime — the library itself never touches tokio.
tokio = { version = "1", features = ["rt", "macros", "time"] }

[lints]
workspace = true
